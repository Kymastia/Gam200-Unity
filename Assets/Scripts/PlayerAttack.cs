using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAttack : MonoBehaviour
{
    //We should not be using a raycast, use a collision detection instead
    //Animations will be complex and that may mean different systems, an adjustable set of collision boxes would be better
    //They should retrieve the stats from a scriptable object.

    //[Header("Raycast")]

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
